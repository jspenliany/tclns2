
set opt(nm)		7		;#  number of mobilenodes
set opt(nb)		9		;#  number of base stations
set opt(nw)		9		;#  number of wired nodes
set opt(x)		1306		;# X dimension of topography
set opt(y)		1306		;# Y dimension of topography
set opt(rowc)		3		;# Y division No.
set opt(colc)		3		;# X division No.
set opt(nodesXx)		0		;# no. of nodes add between X
set opt(nodesYy)		0		;# no. of nodes add between Y
set opt(distnodesXx)		0		;# Distance between no. of nodes add between X
set opt(distnodesYy)		0		;# Distance between no. of nodes add between Y
set opt(juncDelayXx)		0.63367		;# delay between no. of nodes add between X
set opt(juncDelayYy)		0.63367		;# delay between no. of nodes add between Y
set opt(baseDelayXx)		0.0144374		;# delay between no. of nodes add between X
set opt(baseDelayYy)		0.0144374		;# delay between no. of nodes add between Y

